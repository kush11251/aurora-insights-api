# Aurora Insights API
## Overview
Aurora Insights API is a high-performance data analytics platform designed to provide real-time insights and trends. It supports multiple data sources, including CSV, JSON, and database connections.
## Installation
To install the dependencies, run `npm install`.
## Usage
To start the server, run `node src/index.js`. The API will be available at `http://localhost:3000`.